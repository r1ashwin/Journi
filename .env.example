# Local: copy to .env and fill in. Never commit .env.
# Vercel: Project → Settings → Environment Variables (same names, no NEXT_PUBLIC_ prefix).

# Optional — real flight search (https://ignav.com/signup)
IGNAV_API_KEY=

# Optional — if you add AI features later
OPENAI_API_KEY=
